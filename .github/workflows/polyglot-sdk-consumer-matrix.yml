name: Polyglot SDK consumer matrix

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: '47 3 * * 2,5'
  workflow_dispatch:
  repository_dispatch:
    types: [clients-updated]

permissions:
  contents: read

jobs:
  sdk-clients:
    uses: zed-pkg-test/.github/.github/workflows/sdk-client-language-matrix.yml@df3eed96202810b7e7d6667fabc3041b19e6a22f
    with:
      clients_repository: shared-auth/shared-auth-clients
      clients_ref: main
      clients_root: clients
      interfaces_repository: shared-auth/shared-auth-interfaces
      interfaces_ref: main
      interfaces_path: shared-auth-interfaces
      interfaces_dependency: shared-auth/shared-auth-interfaces
      lib_repository: shared-auth/shared-auth-lib
      lib_ref: main
      lib_path: shared-auth-lib
      lib_dependency: shared-auth/shared-auth-lib
    secrets:
      read_token: ${{ secrets.TEST_ORG_READ_TOKEN }}
